#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<< HEAD
  exec "$basedir/node"  "$basedir/../@mapbox/mapbox-gl-style-spec/bin/gl-style-format.js" "$@"
else 
  exec node  "$basedir/../@mapbox/mapbox-gl-style-spec/bin/gl-style-format.js" "$@"
fi
=======
  "$basedir/node"  "$basedir/../@mapbox/mapbox-gl-style-spec/bin/gl-style-format.js" "$@"
  ret=$?
else 
  node  "$basedir/../@mapbox/mapbox-gl-style-spec/bin/gl-style-format.js" "$@"
  ret=$?
fi
exit $ret
>>>>>>> 602cb324b022ea0cd51ae03e04cdbc551bc4e903
